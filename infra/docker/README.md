# Docker setup notes

`docker-compose.yml` at the repo root is the canonical local environment.
This directory is reserved for production-style Dockerfiles and overrides
that aren't needed for development.
