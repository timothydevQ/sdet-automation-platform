# Selenium + JUnit suite

Compatibility smoke. Not a duplicate of the Playwright suite — exists to demonstrate
WebDriver-based automation, classic page object pattern, JUnit 5 lifecycle.

```
mvn -B test
```
